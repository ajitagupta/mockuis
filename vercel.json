{
    "framework": "create-react-app",
    "buildCommand": "npm run build",
    "devCommand": "npm start",
    "outputDirectory": "build",
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }